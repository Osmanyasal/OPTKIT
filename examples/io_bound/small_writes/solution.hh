#ifndef SOLUTION_HH
#define SOLUTION_HH

#include <vector>
#include <string>

// Small frequent writes approach - many individual write operations
void solution(const std::vector<std::string> &data_chunks, const std::string &filename);

#endif // SOLUTION_HH