#ifndef SOLUTION_PATCH_HH
#define SOLUTION_PATCH_HH

#include <vector>
#include <string>

// Optimized buffered I/O approach - uses standard library buffering
void solution_patch(const std::vector<char> &data, const std::string &filename);

#endif // SOLUTION_PATCH_HH