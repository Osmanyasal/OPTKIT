#pragma once

namespace optkit::core::metrics::cpu::intel
{
    /**
     * @brief These are the core events to be monitored for metrics. Events are specific to Intel chips.
     * 
     */
    enum class CoreEvents
    {

    };
}