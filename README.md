# This is an old codebase, search OPTKIT for the new code-base.

## To run 🚀 <br>
```
git clone https://github.com/Osmanyasal/Optimizer-Toolkit-Core.git
cd ./Optimizer-Toolkit-Core
git submodule update --force --recursive --init --remote
make
```

 
